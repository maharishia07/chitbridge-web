import { Layout } from '../components/Layout';
export default function ConnectionsPage() {
  return <Layout title="Connections"><div className="p-4 text-sm text-gray-500">Connections — coming next session</div></Layout>;
}
