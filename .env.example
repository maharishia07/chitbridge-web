# Chit and Bridge Web — Environment Variables
# Copy to .env and fill values

# Backend API URL
VITE_API_URL=http://localhost:3000

# App mode: production | demo | dev
# production = only fully working features shown
# demo       = all screens with visual_flag=true shown
# dev        = everything shown with status badges
VITE_APP_MODE=dev
